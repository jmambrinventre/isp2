name: test build deploy to production

on:
   push: 
    branches: 
      - prueba

jobs:
  test_build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: 16.15.0
      - run: npm i 
      - run: npm test 
      - run: npm run build

  deploy:
    name: deploy to production
    needs: test_build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.12.12 # This is the action
        with:
          heroku_api_key: ${{secrets.HEROKU_API_KEY}}
<<<<<<< HEAD:.github/workflows/integrate.yaml
          heroku_app_name: "prdu" #Must be unique in Heroku
          heroku_email: "jmambrinventre@gmail.com"


=======
          heroku_app_name: "p2is" #Must be unique in Heroku
          heroku_email: "jmambrinventre@gmail.com"  
>>>>>>> develop:.github/workflows/develop.yml
